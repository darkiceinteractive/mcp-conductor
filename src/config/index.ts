/**
 * Configuration — schema validation (Zod), default values, env var overrides,
 * and the config file loader for `~/.mcp-conductor.json`.
 * @module config
 */

export * from './schema.js';
export * from './defaults.js';
export * from './loader.js';
