# Live CLI Test Limitations

These tests spawn real CLIs and fail inside an active Claude Code session (CLAUDECODE=1). Run from a fresh terminal.

Test failures on 2026-07-03 triaged as environmental (nested CLI session + bench flake) — not product bugs.
